This directory holds code that is no longer part of the supported runtime.

As of 2026-05-09, the supported backend runtime is the Nx workspace under
`apps/` and `libs/`. The legacy single-app Nest runtime was archived under
`archive/legacy-src/` to keep migration history without leaving two active
backend entrypoints in the same repo.
